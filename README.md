# Pre-entrega 1.

This installment covers setting up a Node.js project using ESM that implements a ServiceManager class to manage services for a scheduling and reservation system.

# Para ejecutarlo.

1. Create a local `.env` file based on `.env.example`.
2. Install dependencies by running ```npm install```.
3. Run the project with ```npm start```.

